interface:
  display_name: "WxArticle Archive"
  short_description: "Archive WeChat articles and capture public webpage screenshots"
  default_prompt: "Use $wxarticle-archive to archive WeChat article URLs into local Markdown using inline, md-only, full, or rendered fallback mode."
