# 2026-06 Agent Sessions

## 2026-06-15

- Implemented `POST /v3/pages/inline` for generic public webpage archival.
- Added strategies: `auto`, `browser-markdown`, `htmlrewriter`, and `rehype`.
- Added allowlist HTML cleaning, Markdown conversion, metadata/link/image extraction, Browser Run `/markdown` parsing, and diagnostics.
- Added local fixture tests for unsafe URL rejection, sanitizer behavior, static strategies, Browser Run fallback, and HTTP route integration.
- Added remote benchmark script and URL fixture list; benchmark requires `WXARTICLE_API_BASE` and `WXARTICLE_API_KEY`.
- Verification: `npm run typecheck` passed; `npm test` passed; `npm run dry-run` passed with Browser Run, D1, R2, and Queue bindings detected.
- Remote benchmark was initially skipped because process environment variables were absent, then rerun with credentials from `skills/wxarticle-archive/.env.local`.
- Benchmark result: all 40 requests returned HTTP 404 `Not found`, so the configured remote Worker has not deployed the v3 endpoint yet. Browser Run usage remained 0 ms.
- Deployment attempt was blocked because `npx wrangler whoami` reported no authenticated Cloudflare session and no `CLOUDFLARE_API_TOKEN` was present in the process or local ignored env files.
- Updated the root README with a current inventory of production skills, MCP server folders, supporting Worker runtimes, and starter templates.
- Completed Wrangler OAuth login through Chrome; `npx wrangler whoami` confirmed account `Soren`.
- Deployed `wxarticle-archive-api` with Worker version `8ba735b6-0b32-4b4b-b7b4-d98c6c0b34a5`; endpoints include `https://wxarticle-archive-api.sorenliu.workers.dev` and `wxarticle-api.esoren.com`.
- Remote webpage benchmark after deploy: `auto` 10/10 succeeded, 9.257s elapsed, 1.334s Browser Run; `browser-markdown` 10/10 succeeded, 40.631s elapsed, 14.886s Browser Run; `htmlrewriter` 8/10 succeeded, 2.877s elapsed, 0 Browser Run; `rehype` 8/10 succeeded, 3.586s elapsed, 0 Browser Run.
- Benchmark note: static strategies failed on Python.org and W3C due HTTP 403; `auto` fell back to Browser Run for both, though W3C returned a short challenge page titled `Just a moment...`.
- Added challenge-page detection for generic webpage archival. Static challenge pages now trigger `auto` Browser Run fallback, and final challenge pages return HTTP 422 instead of successful archived content.
- Checked Cloudflare dashboard usage after benchmark: Browser Run showed 40 Quick Action requests for Jun 1 through today and browser total hours rounded to 0; benchmark response headers attribute 16.220s of Browser Run time, about 0.0045 browser hours. Worker metrics showed 40 requests on deployed version `8ba735b6`, 0 errors, and low median CPU time.
- Reframed tracked docs, examples, skill instructions, CLI defaults, benchmark env names, and Worker examples around the generic Web Archive/Crawler product. Added `POST /v3/crawl/inline` as the preferred v3 alias while preserving `/v3/pages/inline` and v2 WeChat endpoints.
- Added `WEB_ARCHIVE_API_BASE` / `WEB_ARCHIVE_API_KEY` client and Worker secret aliases while preserving legacy `WXARTICLE_*` compatibility.
- Optimized WeChat article handling by rejecting credentialed URLs, preserving article links in Markdown, normalizing WeChat image CDN URLs to HTTPS, and using a less legacy-specific fetch user agent.
- Verification: Worker `npm run typecheck`, Worker `npm test`, Python client unittest, root `npm run validate`, and Worker `npm run dry-run` passed.
- Tested the real WeChat article `https://mp.weixin.qq.com/s/lNFzhMwxgeo9k5CIvLw0GQ`; static extraction succeeded without Browser Run. Added lightweight image download retries after one concurrent image download was left as a remote WeChat CDN link on the first run; repeat test localized all 13 images.
- Changed local archive layout to one URL-derived folder per archived page, replacing dots with underscores; each page now saves as `<output>/<url-folder>/index.md` with images under `<output>/<url-folder>/images/`. Verified the same WeChat article saves under `mp_weixin_qq_com_s_lNFzhMwxgeo9k5CIvLw0GQ`.
- Added opt-in generic v3 crawl cache/history design: D1 `crawl_urls` and `crawl_snapshots` index metadata/history, while R2 stores full page result snapshots under `crawl/<url-hash>/<snapshot-id>.json`.
- Cache modes now supported: `none`, `fresh`, `reuse-if-fresh`, `stale-while-refresh`, and `history-only`; default v3 inline requests still avoid D1/R2 writes.
- Smoke-tested `https://mp.weixin.qq.com/s/lNFzhMwxgeo9k5CIvLw0GQ` through the deployed WeChat inline route. Result: HTTP 200, title `Codex 急了`, author `Ai学习的老章`, published `2026-06-12 19:09`, 13 images, 1 link, no Browser Run fallback.
- Saved the same article locally through the client. Result: 13 local non-empty image files, 13 local Markdown image references, and 0 remaining WeChat CDN references.
- Smoke-tested the same URL through deployed generic v3. `/v3/pages/inline` returned HTTP 200 with static `htmlrewriter`, 15 images, and 5 links; `/v3/crawl/inline` returned HTTP 404 because that preferred alias is not deployed yet.
- Applied remote D1 crawl-cache schema for `wxarticle_archive` by executing `0004_crawl_cache.sql`, then backfilled Wrangler migration records for 0001-0004 because the existing schema was already present but migration history was empty.
- Deployed Worker version `c85ae5fb-1dc0-4970-becd-15fbaf9c78b5` to `https://wxarticle-archive-api.sorenliu.workers.dev` and custom domain `wxarticle-api.esoren.com`.
- Post-deploy smoke test: `/v3/crawl/inline` returned HTTP 200 on both domains for `https://mp.weixin.qq.com/s/lNFzhMwxgeo9k5CIvLw0GQ`; `store:true` wrote one cache snapshot and `cacheMode:"reuse-if-fresh"` returned a cache hit in 526 ms.

## 2026-06-22

- Imported the local AutoDL API skill from `C:\Cloud\S3-R2\skills\autodl-api` into `skills/autodl-api/`.
- Added the skill to the README inventory.
- Verification: `npm run validate` passed.

## 2026-06-30

- Imported local DIDA skills into `skills/dida-cli/` and `skills/dida365-task-manager/`.
- Imported current Flomo MCP skills into `skills/flomo-mcp-guide/`, `skills/flomo-memo-create/`, `skills/flomo-tag-maintainer/`, and `skills/flomo-daily-review-writer/`.
- Imported the PowerShell command-authoring skill into `skills/powershell-shell/`.
- Updated the README skill inventory and agent requirements log.
- Narrowed placeholder detection in `scripts/validate-repo.mjs` so legitimate lowercase `todo` trigger text is not treated as an unfinished `TODO`.
- Verification: `npm run validate` passed; skill secret/cache scan found no real tokens, local config, `.env`, `.pyc`, or `__pycache__` files.
