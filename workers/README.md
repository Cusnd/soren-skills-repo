# Workers

Cloudflare Worker projects that support repository skills live here.

Rules:

- Commit source, migrations, tests, package manifests, lockfiles, and sanitized examples.
- Do not commit real `wrangler.jsonc`, `.dev.vars`, account IDs, production database IDs, API keys, or generated bundles.
- Put setup and deploy instructions in each Worker README.
